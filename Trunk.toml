[build]
target = "index.html"
dist = "dist"
# Use relative paths for GitHub Pages subdirectory
public_url = "./"

[watch]
watch = ["src", "index.html", "style.css"]

[serve]
addresses = ["127.0.0.1", "::1"]
port = 8080
